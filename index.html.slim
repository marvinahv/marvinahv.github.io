doctype html
html lang="en"
  head
    meta charset="utf-8"
    meta name="description" content="Tree documents for clarity focus and flow"
    meta name="viewport" content="initial-scale=1, user-scalable=no"
    title Marvin Alejandro Herrera
    link rel="stylesheet" href="http://yui.yahooapis.com/pure/0.6.0/pure-min.css"
    / link rel='stylesheet' href='http://fonts.googleapis.com/css?family=Oswald|Muli:400,300'
    link rel='stylesheet' href='http://fonts.googleapis.com/css?family=Alegreya:400,700|Roboto:400,300,100|Roboto+Condensed:400,300'
    link rel="stylesheet" href="/css/main.css"

  body
    #main
      .intro
        h1 Marvin Alejandro Herrera
        p I architect, design and build web applications. I like making things that make the world better. I believe the whole is greater than the sum of its parts. Minimalism and simplicity are awesome.


      section.experience
        .pure-g
          .pure-u-1-3
          .pure-u-2-3
            h2.right Experience

        - @experience.each do |item|
          - item = item.to_ostruct

          .experience-item.pure-g
            .pure-u-1-3
              .date = item.date

            .pure-u-2-3
              h3 = item.title

              - if item.company
                .company = item.company

              == RedCloth.new(item.desc).to_html



      section.skills
        .pure-g
          .pure-u-1-3
            h2.left Skills

        - @skills.map!{ |skill| skill.to_ostruct }

        .skills.pure-g
          .skill.pure-u-2-3
            h3 = @skills[0].title
            .desc.right-pad == RedCloth.new(@skills[0].desc).to_html

          .skill.pure-u-1-3
            h3 = @skills[1].title
            .desc == RedCloth.new(@skills[1].desc).to_html



      section.portfolio
        .pure-g
          .pure-u-1-3
          .pure-u-2-3
            h2.right Some Works

        - @portfolio.each do |item|
          - item = item.to_ostruct

          .portfolio-item.pure-g
            .pure-u-1-3
              .work == RedCloth.new(item.work).to_html
              p.stack.right-pad = item.stack

            .pure-u-2-3
              h3 = item.title
              .contact = item.contact
              p = item.desc



      section.recommendations
        .pure-g
          .pure-u-1-3
            h2.left Recommendations

        - @recommendations.each do |recommendation|
          - recommendation = recommendation.to_ostruct

          .recommendation.pure-g
            .pure-u-2-3
              p "#{recommendation.quote}"

            .pure-u-1-3
              .left-pad.person
                h3
                  a href=recommendation.url target="_blank" = recommendation.name
                .title = recommendation.title